# Flows live one folder per feature under flows/, with property flows belonging
# to no feature directly inside it. Both levels are globbed explicitly rather
# than recursively, so that fragments in subflows/ stay outside the glob and are
# never run as flows of their own.
flows:
  - "flows/*.yaml"
  - "flows/*/*.yaml"
