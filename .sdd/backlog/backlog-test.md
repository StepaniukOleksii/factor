# Backlog — Tests

This is an informal idea/issue capture list — not a specification. It exists to hold loose testing
notes until they're ready to become a real spec.

## E2E flows to write

- `.maestro/2-9-record-note.yaml`, on the `seed` fixture — adding a Record to `mixed metrics` with a note
  and reading it back on its expanded row. Named by
  [Record Note](../epics/2-record-management/2-9-record-note/spec.md) §4, whose implementation is
  otherwise complete.
