# Unparented

## Goal
Holds features that don't belong to a specific epic. This is the default home for a new feature — it only moves
into a numbered epic when several features share a genuinely common, bounded outcome worth tracking together.
