# Record Management

## Goal
Cover the full lifecycle of a Record: creating it, presenting available actions, deleting it, and editing it.
Grouped as one epic because these features together define the baseline CRUD surface for the Record entity,
mirroring Observation Management for the app's second core entity.
